Put the two compile-only jars here (they are not redistributable, so they are not in the repo):

- `the_vault-*.jar`
- `vhapi-*.jar`

Copy them from the `mods` folder of your Wold's Vaults instance, then set `vault_jar` / `vhapi_jar`
in `gradle.properties` to their exact file names.
